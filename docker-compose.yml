services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      # Override secrets in production; these map directly to Spring Boot properties.
      APP_JWT_SECRET: "dev-secret-change-later-12345678901234567890"
      APP_JWT_EXPIRATION_SECONDS: "3600"
    restart: unless-stopped
